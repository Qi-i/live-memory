from pathlib import Path
import re

ROOT = Path(__file__).resolve().parents[1]


def read(path):
    return (ROOT / path).read_text(encoding="utf-8")


def write(path, text):
    (ROOT / path).write_text(text, encoding="utf-8")


def replace_once(text, old, new, label):
    if old not in text:
        raise RuntimeError(f"missing replacement target: {label}")
    return text.replace(old, new, 1)


def sub_once(text, pattern, replacement, label):
    next_text, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
    if count != 1:
        raise RuntimeError(f"regex replacement failed ({count}): {label}")
    return next_text

# domain.ts
path = "src/domain.ts"
text = read(path)
text = replace_once(
    text,
    'export type SourceChannel = "" | "damai" | "fenwandao" | "maoyan" | "official" | "onsite" | "transfer" | "other";',
    'export type SourceChannel = "" | "damai" | "fenwandao" | "piaoxingqiu" | "maoyan" | "official" | "onsite" | "transfer" | "other";',
    "source channel",
)
text = replace_once(text, '  fenwandao: "纷玩岛",\n  maoyan: "猫眼",', '  fenwandao: "纷玩岛",\n  piaoxingqiu: "票星球",\n  maoyan: "猫眼",', "source labels")
write(path, text)

# importers.ts
path = "src/importers.ts"
text = read(path)
text = sub_once(
    text,
    r'export async function createDraftsFromText\(text: string\): Promise<ImportDraft\[]> \{.*?\n\}\n\nexport async function fetchDamaiDraft',
    '''export type TicketingPlatform = "damai" | "fenwandao" | "piaoxingqiu";\n\nexport function detectTicketingPlatform(url: string): TicketingPlatform | "" {\n  if (/damai\\.cn/i.test(url)) return "damai";\n  if (/livelab\\.com\\.cn|fenwandao/i.test(url)) return "fenwandao";\n  if (/piaoxingqiu\\.com/i.test(url)) return "piaoxingqiu";\n  return "";\n}\n\nexport async function createDraftsFromText(text: string): Promise<ImportDraft[]> {\n  const urls = extractUrls(text);\n  const drafts: ImportDraft[] = [];\n  for (const url of urls) {\n    const platform = detectTicketingPlatform(url);\n    if (platform === "damai") {\n      drafts.push(await fetchDamaiDraft(url));\n    } else if (platform) {\n      drafts.push(await fetchTicketingDraft(url, platform));\n    } else {\n      drafts.push(basicUrlDraft(url));\n    }\n  }\n  if (!drafts.length && text.trim()) drafts.push(parsePlainTextDraft(text));\n  return drafts;\n}\n\nexport async function fetchDamaiDraft''',
    "create drafts multi-platform",
)

insert = '''async function fetchTicketingDraft(url: string, platform: Exclude<TicketingPlatform, "damai">): Promise<ImportDraft> {\n  const fallback = basicUrlDraft(url, platform);\n  try {\n    const readerUrl = `https://r.jina.ai/http://${url.replace(/^https?:\\/\\//i, "")}`;\n    const response = await fetchWithTimeout(readerUrl, 14000);\n    if (!response.ok) throw new Error(`${platform} 页面读取失败：${response.status}`);\n    const pageText = await response.text();\n    return {\n      ...fallback,\n      ...parseTicketingReaderText(pageText, url, platform),\n      sourceUrl: url,\n      sourceChannel: platform,\n    };\n  } catch {\n    return fallback;\n  }\n}\n\nexport function parseTicketingReaderText(text: string, url: string, platform: TicketingPlatform): Partial<ImportDraft> {\n  if (platform === "damai") {\n    return { ...parseDamaiReaderText(text, url), sourceChannel: "damai", sourceUrl: url };\n  }\n  const structuredTitle =\n    jsonStringForKey(text, "itemName") ||\n    jsonStringForKey(text, "performanceName") ||\n    jsonStringForKey(text, "showName") ||\n    jsonStringForKey(text, "projectName");\n  const readerTitle =\n    /Title:\\s*(.+)/i.exec(text)?.[1]?.trim() ||\n    /#\\s+(.+)/.exec(text)?.[1]?.trim() ||\n    /(?:演出名称|项目名称)[:：]\\s*([^\\n]+)/.exec(text)?.[1]?.trim() ||\n    "";\n  const title = cleanupTicketingTitle(structuredTitle || readerTitle, platform);\n  const showTime =\n    jsonStringForKey(text, "showTime") ||\n    jsonStringForKey(text, "startTime") ||\n    jsonStringForKey(text, "performanceTime") ||\n    jsonStringForKey(text, "showDate") ||\n    /(?:演出时间|演出日期|时间)[:：]\\s*([^\\n]+)/.exec(text)?.[1] ||\n    "";\n  const venue = cleanVenue(\n    jsonStringForKey(text, "venueName") ||\n    jsonStringForKey(text, "stadiumName") ||\n    jsonStringForKey(text, "venueTitle") ||\n    /(?:演出场馆|场馆|演出地点|地点)[:：]\\s*([^\\n]+)/.exec(text)?.[1] ||\n    "",\n  );\n  const city = normalizeCity(\n    jsonStringForKey(text, "venueCityName") ||\n    jsonStringForKey(text, "cityName") ||\n    inferCity(`${title} ${venue} ${text}`),\n  );\n  const priceLine =\n    jsonStringForKey(text, "priceRange") ||\n    /(?:票档|票价)[:：]\\s*([^\\n]+)/.exec(text)?.[1] ||\n    "";\n  const posterUrl = findTicketingPoster(text);\n  const date = normalizeDate(showTime || text);\n  const fields = [title, date, venue, posterUrl].filter(Boolean).length;\n  return {\n    title: title || (platform === "fenwandao" ? "纷玩岛项目" : "票星球项目"),\n    category: /音乐节|festival/i.test(title) ? "festival" : "concert",\n    status: normalizeStatus(undefined, date),\n    date: date || new Date().toISOString().slice(0, 10),\n    time: normalizeTime(showTime || text),\n    city,\n    venue,\n    artists: inferArtists(title),\n    publicPriceRange: priceLine.slice(0, 120),\n    posterUrl: posterUrl || undefined,\n    sourceChannel: platform,\n    sourceUrl: url,\n    importConfidence: fields >= 4 ? 0.94 : fields >= 3 ? 0.86 : fields >= 2 ? 0.72 : 0.3,\n  };\n}\n\nfunction cleanupTicketingTitle(value: string, platform: TicketingPlatform) {\n  const platformName = platform === "fenwandao" ? "纷玩岛" : platform === "piaoxingqiu" ? "票星球" : "大麦";\n  return cleanupDamaiTitle(value)\n    .replace(new RegExp(`(?:\\\\s*[-—–_|｜]\\\\s*)?${platformName}(?:官网|官方)?\\\\s*$`, "i"), "")\n    .replace(/\\s{2,}/g, " ")\n    .trim();\n}\n\n'''
text = replace_once(text, 'export function parseDamaiReaderText(text: string, url: string): Partial<ImportDraft> {', insert + 'export function parseDamaiReaderText(text: string, url: string): Partial<ImportDraft> {', "ticket reader parser")

find_poster = '''function findTicketingPoster(text: string) {\n  for (const key of ["itemPic", "posterUrl", "posterPic", "verticalPic", "projectPic", "coverUrl", "coverImage", "imageUrl", "performancePic"]) {\n    const value = cleanImageUrl(jsonStringForKey(text, key));\n    if (isUsablePoster(value)) return value;\n  }\n  for (const key of ["picUrl", "image", "url"]) {\n    const value = allJsonStringsForKey(text, key).map(cleanImageUrl).find(isUsablePoster);\n    if (value) return value;\n  }\n  return bestMarkdownImage(text, false);\n}\n\n'''
text = replace_once(text, 'function findDamaiPoster(text: string) {', find_poster + 'function findDamaiPoster(text: string) {', "generic poster finder")

text = sub_once(
    text,
    r'function basicUrlDraft\(url: string, sourceChannel: ImportDraft\["sourceChannel"\] = ""\): ImportDraft \{.*?\n\}',
    '''function basicUrlDraft(url: string, sourceChannel: ImportDraft["sourceChannel"] = ""): ImportDraft {\n  const platform = sourceChannel || detectTicketingPlatform(url);\n  const title = platform === "damai" ? "大麦项目" : platform === "fenwandao" ? "纷玩岛项目" : platform === "piaoxingqiu" ? "票星球项目" : "链接导入项目";\n  return {\n    id: createId("draft"),\n    title,\n    category: "concert",\n    status: "planned",\n    date: new Date().toISOString().slice(0, 10),\n    city: "",\n    venue: "",\n    artists: [],\n    sourceChannel: platform,\n    sourceUrl: url,\n    importConfidence: 0.25,\n  };\n}''',
    "basic url draft",
)

text = sub_once(
    text,
    r'function parsePlainTextDraft\(text: string\): ImportDraft \{.*?\n\}',
    '''function parsePlainTextDraft(text: string): ImportDraft {\n  const lines = text.split(/\\n/).map((line) => line.trim()).filter(Boolean);\n  const firstLine = lines.find((line) => !/^(大麦|纷玩岛|票星球|演出详情|订单详情)$/.test(line)) || "文本导入项目";\n  const date = normalizeDate(text) || new Date().toISOString().slice(0, 10);\n  const category: EventCategory = /音乐节/i.test(text) ? "festival" : "concert";\n  const status: EventStatus = normalizeStatus(undefined, date);\n  const venue = /(?:演出场馆|场馆|演出地点|地点)[:：]?\\s*([^\\n]+)/.exec(text)?.[1]?.trim() || "";\n  const publicPriceRange = /(?:票档|票价)[:：]?\\s*([^\\n]+)/.exec(text)?.[1]?.trim() || "";\n  const sourceChannel: ImportDraft["sourceChannel"] = /纷玩岛/.test(text) ? "fenwandao" : /票星球/.test(text) ? "piaoxingqiu" : /大麦/.test(text) ? "damai" : "";\n  const usefulFields = [date, venue, inferCity(text)].filter(Boolean).length;\n  return {\n    id: createId("draft"),\n    title: firstLine.slice(0, 80),\n    category,\n    status,\n    date,\n    time: normalizeTime(text),\n    city: inferCity(text),\n    venue,\n    artists: inferArtists(firstLine),\n    publicPriceRange: publicPriceRange.slice(0, 120),\n    sourceChannel,\n    importConfidence: usefulFields >= 2 ? 0.62 : 0.45,\n  };\n}''',
    "plain text screenshot parser",
)
write(path, text)

# media.ts
path = "src/media.ts"
text = read(path)
text = sub_once(
    text,
    r'export async function compressImage\(file: File, maxEdge = 1800, quality = 0\.88\) \{.*?\n\}\n\nexport function readAsDataUrl\(file: File\): Promise<string> \{',
    '''export async function compressImage(file: File, maxEdge = 1800, quality = 0.88) {\n  let source: CanvasImageSource | null = null;\n  let sourceWidth = 0;\n  let sourceHeight = 0;\n  let bitmap: ImageBitmap | null = null;\n  let fallbackSrc = "";\n\n  if (typeof createImageBitmap === "function") {\n    try {\n      bitmap = await createImageBitmap(file);\n      source = bitmap;\n      sourceWidth = bitmap.width;\n      sourceHeight = bitmap.height;\n    } catch {\n      bitmap = null;\n    }\n  }\n\n  if (!source) {\n    fallbackSrc = await readAsDataUrl(file);\n    const image = await loadImage(fallbackSrc);\n    source = image;\n    sourceWidth = image.naturalWidth;\n    sourceHeight = image.naturalHeight;\n  }\n\n  const ratio = Math.min(1, maxEdge / Math.max(sourceWidth, sourceHeight));\n  const width = Math.max(1, Math.round(sourceWidth * ratio));\n  const height = Math.max(1, Math.round(sourceHeight * ratio));\n  const canvas = document.createElement("canvas");\n  canvas.width = width;\n  canvas.height = height;\n  const context = canvas.getContext("2d");\n  if (!context) {\n    bitmap?.close();\n    const src = fallbackSrc || await readAsDataUrl(file);\n    return { src, width: sourceWidth, height: sourceHeight, mimeType: file.type, size: file.size };\n  }\n  context.drawImage(source, 0, 0, width, height);\n  const mimeType = file.type === "image/png" ? "image/png" : "image/jpeg";\n  const blob = await canvasToBlob(canvas, mimeType, quality);\n  bitmap?.close();\n  if (!blob) {\n    const src = fallbackSrc || await readAsDataUrl(file);\n    return { src, width, height, mimeType: file.type, size: file.size };\n  }\n  const dataUrl = await readAsDataUrl(blob);\n  return { src: dataUrl, width, height, mimeType: blob.type || mimeType, size: blob.size };\n}\n\nfunction canvasToBlob(canvas: HTMLCanvasElement, mimeType: string, quality: number) {\n  return new Promise<Blob | null>((resolve) => canvas.toBlob(resolve, mimeType, quality));\n}\n\nexport function readAsDataUrl(file: Blob): Promise<string> {''',
    "async image compression",
)
write(path, text)

# supabase.ts
path = "src/supabase.ts"
text = read(path)
helper = '''function canonicalSyncRecord(record: EventRecord, includeMedia: boolean) {\n  const { syncedAt: _syncedAt, updatedAt: _updatedAt, media, ...rest } = record;\n  const normalizedMedia = includeMedia\n    ? media.map((asset) => {\n      const { updatedAt: _assetUpdatedAt, source: _assetSource, src, ...stable } = asset;\n      return { ...stable, src: asset.storagePath ? "" : src };\n    }).sort((a, b) => String(a.id).localeCompare(String(b.id)))\n    : [];\n  return { ...rest, media: normalizedMedia };\n}\n\nfunction sortForStableJson(value: unknown): unknown {\n  if (Array.isArray(value)) return value.map(sortForStableJson);\n  if (!value || typeof value !== "object") return value;\n  return Object.fromEntries(Object.entries(value as Record<string, unknown>)\n    .sort(([a], [b]) => a.localeCompare(b))\n    .map(([key, item]) => [key, sortForStableJson(item)]));\n}\n\nexport function recordsSemanticallyEqual(local: EventRecord, cloud: EventRecord, includeMedia = true) {\n  return JSON.stringify(sortForStableJson(canonicalSyncRecord(local, includeMedia)))\n    === JSON.stringify(sortForStableJson(canonicalSyncRecord(cloud, includeMedia)));\n}\n\n'''
text = replace_once(text, 'async function syncAccountTextBackup(settings: AppSettings, localRecords: EventRecord[]): Promise<AutoSyncResult> {', helper + 'async function syncAccountTextBackup(settings: AppSettings, localRecords: EventRecord[]): Promise<AutoSyncResult> {', "semantic sync helper")

text = sub_once(
    text,
    r'async function syncAccountTextBackup\(settings: AppSettings, localRecords: EventRecord\[]\): Promise<AutoSyncResult> \{.*?\n\}\n\nasync function syncPersonalSupabase',
    '''async function syncAccountTextBackup(settings: AppSettings, localRecords: EventRecord[]): Promise<AutoSyncResult> {\n  const client = makeAccountClient(settings);\n  const user = await requireUser(client);\n  const { data, error } = await client\n    .from("echo_text_backups")\n    .select("payload, updated_at, deleted_at")\n    .eq("user_id", user.id)\n    .order("updated_at", { ascending: false });\n  if (error) throwTextBackupError(error);\n\n  const cloudRecords = (data || []).map((row) => normalizeRecord({\n    ...(row.payload as EventRecord),\n    updatedAt: String(row.updated_at || (row.payload as EventRecord).updatedAt),\n    deletedAt: row.deleted_at ? String(row.deleted_at) : undefined,\n  }));\n  const localById = new Map(localRecords.map((record) => [record.id, record]));\n  const cloudById = new Map(cloudRecords.map((record) => [record.id, record]));\n  const merged = new Map(localById);\n  const toPush: EventRecord[] = [];\n\n  // Account backup is secondary storage. Compare actual content/timestamps and do not\n  // reuse the personal-cloud syncedAt marker, otherwise one backend can suppress the other.\n  for (const [id, local] of localById) {\n    const cloud = cloudById.get(id);\n    if (!cloud) {\n      toPush.push(local);\n      continue;\n    }\n    if (recordsSemanticallyEqual(local, cloud, false)) continue;\n    if (cloud.updatedAt > local.updatedAt) {\n      merged.set(id, normalizeRecord({ ...cloud, media: local.media, syncedAt: local.syncedAt }));\n    } else {\n      toPush.push(local);\n    }\n  }\n\n  for (const [id, cloud] of cloudById) {\n    if (!localById.has(id)) merged.set(id, normalizeRecord({ ...cloud, media: [] }));\n  }\n\n  if (toPush.length) {\n    const rows = toPush.map((record) => ({\n      id: record.id,\n      user_id: user.id,\n      payload: withoutLocalMedia(record),\n      updated_at: record.updatedAt,\n      deleted_at: record.deletedAt || null,\n    }));\n    const { error: pushError } = await client.from("echo_text_backups").upsert(rows, { onConflict: "user_id,id" });\n    if (pushError) throwTextBackupError(pushError);\n  }\n\n  return {\n    records: Array.from(merged.values()).sort((a, b) => b.date.localeCompare(a.date)),\n    conflicts: [],\n    message: toPush.length ? `已备份 ${toPush.length} 条文字记录` : "",\n  };\n}\n\nasync function syncPersonalSupabase''',
    "account sync lww",
)

text = replace_once(
    text,
    '  const cloudRecords = (data || []).map((row) => normalizeRecord({\n    ...(row.payload as EventRecord),\n    deletedAt: row.deleted_at ? String(row.deleted_at) : undefined,\n  }));\n\n  const localById = new Map(localRecords.map((r) => [r.id, r]));',
    '  const cloudRecords = (data || []).map((row) => normalizeRecord({\n    ...(row.payload as EventRecord),\n    updatedAt: String(row.updated_at || (row.payload as EventRecord).updatedAt),\n    deletedAt: row.deleted_at ? String(row.deleted_at) : undefined,\n  }));\n\n  const localById = new Map(localRecords.map((r) => [r.id, r]));',
    "personal cloud updated_at",
)

text = replace_once(
    text,
    '    const syncedAt = local.syncedAt;\n    if (syncedAt) {',
    '    if (recordsSemanticallyEqual(local, cloud, true)) {\n      merged.set(id, normalizeRecord({ ...local, syncedAt: nowIso() }));\n      continue;\n    }\n    const syncedAt = local.syncedAt;\n    if (syncedAt) {',
    "personal semantic equality",
)

text = sub_once(
    text,
    r'  // Push local changes\n  if \(toPush\.length\) \{\n    for \(const record of toPush\) \{.*?\n    \}\n  \}\n\n  const records = Array\.from\(merged\.values\(\)\)',
    '''  // Push local changes. Upload media first so the cloud payload and local state\n  // both receive storagePath values in the same transaction cycle.\n  if (toPush.length) {\n    const bucket = mediaBucket(settings);\n    for (const record of toPush) {\n      let media = record.media;\n      if (settings.supabase.syncMedia && !record.deletedAt) {\n        media = await Promise.all(record.media.map(async (asset) => {\n          try {\n            return await uploadMediaIfNeeded(client, ownerKey, record.id, asset, bucket);\n          } catch {\n            return asset;\n          }\n        }));\n      }\n      const syncedRecord = normalizeRecord({ ...record, media, syncedAt: nowIso() });\n      const cloudPayload = settings.supabase.syncMedia ? cloudRecordPayload(syncedRecord) : withoutLocalMedia(syncedRecord);\n      const { error: pushError } = await client.from("echo_passkey_records").upsert(\n        {\n          id: syncedRecord.id,\n          owner_key: ownerKey,\n          payload: cloudPayload,\n          updated_at: syncedRecord.updatedAt,\n          deleted_at: syncedRecord.deletedAt || null,\n        },\n        { onConflict: "owner_key,id" },\n      );\n      if (pushError) throwPersonalCloudError(pushError);\n      merged.set(record.id, syncedRecord);\n    }\n  }\n\n  const records = Array.from(merged.values())''',
    "personal media upload ordering",
)

text = replace_once(text, '    const next = normalizeRecord({ ...record, media, updatedAt: nowIso() });', '    const next = normalizeRecord({ ...record, media, syncedAt: nowIso() });', "manual push preserves updatedAt")
text = replace_once(
    text,
    '    media: record.media.map((asset) => asset.storagePath\n      ? { ...asset, src: "", source: "supabase" as const }\n      : asset),',
    '    media: record.media.map((asset) => asset.storagePath\n      ? { ...asset, src: "", source: "supabase" as const }\n      : asset.src.startsWith("data:") ? { ...asset, src: "" } : asset),',
    "cloud payload excludes data urls",
)
write(path, text)

# appController.ts
path = "src/appController.ts"
text = read(path)
marker = '''function mediaPathFingerprint(records: EventRecord[]) {\n  return records.flatMap((record) => record.media.filter((asset) => asset.storagePath).map((asset) => `${record.id}:${asset.id}:${asset.storagePath}`)).join("|");\n}\n'''
addition = marker + '''\nexport function autoSyncFingerprint(records: EventRecord[], settings: AppSettings) {\n  const recordPart = recordFingerprint(records);\n  const mediaPart = records.flatMap((record) => record.media.map((asset) =>\n    `${record.id}:${asset.id}:${asset.storagePath || (asset.src.startsWith("data:") ? "local-data" : asset.src)}`,\n  )).join("|");\n  const cloudPart = [\n    hasPersonalCloudConnection(settings) ? "personal" : "",\n    settings.supabase.url.trim(),\n    settings.supabase.ownerKey ? "connected" : "",\n    settings.supabase.syncMedia ? "media" : "text",\n    hasAccountCloudConfig(settings) ? "account" : "",\n  ].join(":");\n  return `${recordPart}::${mediaPart}::${cloudPart}`;\n}\n'''
text = replace_once(text, marker, addition, "auto sync fingerprint helper")
text = replace_once(text, '        lastSyncFingerprint.current = recordFingerprint(nextRecords);\n        initialized.current = true;', '        lastSyncFingerprint.current = "";\n        initialized.current = true;', "initial auto sync")
text = replace_once(text, '    const fingerprint = recordFingerprint(records);', '    const fingerprint = autoSyncFingerprint(records, settings);', "effect fingerprint")
text = replace_once(text, '          const nextFingerprint = recordFingerprint(result.records);', '          const nextFingerprint = autoSyncFingerprint(result.records, settings);', "result fingerprint")
text = replace_once(
    text,
    '          lastSyncFingerprint.current = nextFingerprint;\n          if (result.message) flash(result.message);',
    '          lastSyncFingerprint.current = nextFingerprint;\n          const syncedSettings = writeSettings({ ...settings, lastSyncAt: nowIso() });\n          setSettings(syncedSettings);\n          if (result.message) flash(result.message);',
    "record automatic sync time",
)
write(path, text)

# settingsPage.tsx
path = "src/settingsPage.tsx"
text = read(path)
text = replace_once(text, '  pullRecordsFromSupabase,\n  pushRecordsToSupabase,\n  refreshSignedMediaUrls,', '  pullRecordsFromSupabase,\n  refreshSignedMediaUrls,', "remove manual push import")
text = sub_once(text, r'\n  async function uploadCloud\(\) \{.*?\n  \}\n', '\n', "remove manual upload function")
text = replace_once(text, 'description="连接自己的 Supabase 后，文字和图片可以在电脑与手机之间同步。"', 'description="连接自己的 Supabase 后，新增与修改会自动同步；无需手动上传整份档案。"', "cloud module description")
text = replace_once(text, '              <button className="button ghost" type="button" disabled={busy || !cloudConnected} onClick={() => void run(uploadCloud)}><Upload />上传当前档案</button>\n', '', "remove manual upload button")
write(path, text)

# overlays.tsx
path = "src/overlays.tsx"
text = read(path)
text = replace_once(text, 'import { fileToMedia, makeMedia, nowIso } from "./media";', 'import { fileToMedia, makeMedia, nowIso } from "./media";\nimport { recognizeTicketScreenshot } from "./ocr";', "ocr import")

record_editor = '''type MediaProcessState = "idle" | "processing" | "ready" | "error";\n\nexport function RecordEditor({ record, onCancel, onSave }: { record: EventRecord; onCancel: () => void; onSave: (record: EventRecord) => Promise<unknown> }) {\n  const [draft, setDraft] = useState(record);\n  const [saving, setSaving] = useState(false);\n  const [mediaStates, setMediaStates] = useState<Partial<Record<MediaKind, MediaProcessState>>>({});\n  const [mediaErrors, setMediaErrors] = useState<Partial<Record<MediaKind, string>>>({});\n  const [recognizing, setRecognizing] = useState(false);\n  const [recognitionStatus, setRecognitionStatus] = useState("");\n  useEffect(() => {\n    setDraft(record);\n    setMediaStates({});\n    setMediaErrors({});\n    setRecognitionStatus("");\n  }, [record]);\n\n  async function addFiles(kind: MediaKind, files: FileList | null) {\n    if (!files?.length) return;\n    setMediaStates((current) => ({ ...current, [kind]: "processing" }));\n    setMediaErrors((current) => ({ ...current, [kind]: "" }));\n    try {\n      const media = await Promise.all(Array.from(files).map((file) => fileToMedia(draft.id, kind, file)));\n      setDraft((current) => ({\n        ...current,\n        media: kind === "poster" || kind === "ticket" || kind === "seatMap"\n          ? current.media.filter((item) => item.kind !== kind).concat(media)\n          : current.media.concat(media),\n      }));\n      setMediaStates((current) => ({ ...current, [kind]: "ready" }));\n    } catch (error) {\n      setMediaStates((current) => ({ ...current, [kind]: "error" }));\n      setMediaErrors((current) => ({ ...current, [kind]: error instanceof Error ? error.message : "图片处理失败" }));\n    }\n  }\n\n  function removeMedia(id: string) {\n    setDraft((current) => ({ ...current, media: current.media.filter((item) => item.id !== id) }));\n  }\n\n  function applyRecognizedDraft(imported: ImportDraft) {\n    setDraft((current) => mergeImportDraftIntoRecord(current, imported));\n    if (imported.importConfidence >= 0.4) {\n      setRecognitionStatus(`识别完成 · ${sourceLabels[imported.sourceChannel]} · 置信度 ${Math.round(imported.importConfidence * 100)}%`);\n    } else if (imported.posterUrl) {\n      setRecognitionStatus("已获取海报，页面其他字段受平台限制，请手动核对");\n    } else {\n      setRecognitionStatus("链接已保存，但暂未读取到足够的演出信息");\n    }\n  }\n\n  async function recognizeSourceLink() {\n    if (!draft.sourceUrl?.trim()) return;\n    setRecognizing(true);\n    setRecognitionStatus("正在读取票务页面…");\n    try {\n      const imported = (await createDraftsFromText(draft.sourceUrl.trim()))[0];\n      if (!imported) throw new Error("没有识别到演出信息");\n      applyRecognizedDraft(imported);\n    } catch (error) {\n      setRecognitionStatus(error instanceof Error ? error.message : "链接识别失败");\n    } finally {\n      setRecognizing(false);\n    }\n  }\n\n  async function recognizeScreenshot(file?: File) {\n    if (!file) return;\n    setRecognizing(true);\n    setRecognitionStatus("正在准备截图识别…");\n    try {\n      const recognized = await recognizeTicketScreenshot(file, (progress, status) => {\n        setRecognitionStatus(`${status} ${Math.round(progress * 100)}%`);\n      });\n      const imported = (await createDraftsFromText(recognized))[0];\n      if (!imported) throw new Error("截图中没有识别到演出信息");\n      applyRecognizedDraft(imported);\n    } catch (error) {\n      setRecognitionStatus(error instanceof Error ? error.message : "截图识别失败");\n    } finally {\n      setRecognizing(false);\n    }\n  }\n\n  async function submit(event: FormEvent) {\n    event.preventDefault();\n    if (Object.values(mediaStates).some((state) => state === "processing")) return;\n    setSaving(true);\n    try {\n      await onSave({ ...draft, lineup: draft.artists.map((name) => ({ name, role: "artist" })), updatedAt: nowIso() });\n    } finally {\n      setSaving(false);\n    }\n  }\n\n  const mediaFor = (kind: MediaKind) => draft.media.filter((item) => item.kind === kind);\n  const mediaBusy = Object.values(mediaStates).some((state) => state === "processing");\n\n  return (\n    <div className="overlay-backdrop editor-backdrop-v2">\n      <form className="record-editor-v2" onSubmit={submit}>\n        <header className="editor-header-v2"><div><span>RECORD EDITOR</span><h2>{record.title ? "编辑演出" : "新增演出"}</h2></div><button type="button" aria-label="关闭" onClick={onCancel}><X /></button></header>\n        <div className="editor-grid-v2">\n          <EditorField className="is-wide" label="演出名称"><input required value={draft.title} onChange={(event) => setDraft({ ...draft, title: event.target.value })} /></EditorField>\n          <EditorField className="is-wide" label="艺人 / 阵容"><input value={draft.artists.join(" / ")} onChange={(event) => setDraft({ ...draft, artists: splitTextList(event.target.value) })} placeholder="多个用 / 或 、 分隔" /></EditorField>\n          <EditorField label="类型"><select value={draft.category} onChange={(event) => setDraft({ ...draft, category: event.target.value as EventCategory })}>{Object.entries(categoryLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></EditorField>\n          <EditorField label="状态"><select value={draft.status} onChange={(event) => setDraft({ ...draft, status: event.target.value as EventStatus })}>{Object.entries(statusLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></EditorField>\n          <EditorField label="日期"><input type="date" value={draft.date} onChange={(event) => setDraft({ ...draft, date: event.target.value })} /></EditorField>\n          <EditorField label="时间"><input type="time" value={draft.time || ""} onChange={(event) => setDraft({ ...draft, time: event.target.value })} /></EditorField>\n          <EditorField label="城市"><input value={draft.city} onChange={(event) => setDraft({ ...draft, city: event.target.value })} /></EditorField>\n          <EditorField label="场馆"><input value={draft.venue} onChange={(event) => setDraft({ ...draft, venue: event.target.value })} /></EditorField>\n          <EditorField label="票价"><input type="number" value={draft.price ?? ""} onChange={(event) => setDraft({ ...draft, price: event.target.value ? Number(event.target.value) : null })} /></EditorField>\n          <EditorField label="公开票价"><input value={draft.publicPriceRange || ""} onChange={(event) => setDraft({ ...draft, publicPriceRange: event.target.value })} /></EditorField>\n          <EditorField className="is-wide" label="座位"><input value={draft.seat || ""} onChange={(event) => setDraft({ ...draft, seat: event.target.value })} /></EditorField>\n          <EditorField className="is-wide" label="同行人"><input value={draft.companions.join(" / ")} onChange={(event) => setDraft({ ...draft, companions: splitTextList(event.target.value) })} /></EditorField>\n          <EditorField className="is-wide" label="标签"><input value={draft.tags.join(" / ")} onChange={(event) => setDraft({ ...draft, tags: splitTextList(event.target.value) })} /></EditorField>\n          <EditorField className="is-wide" label="票务链接 / 来源">\n            <div className="editor-source-import-v2">\n              <input value={draft.sourceUrl || ""} onChange={(event) => setDraft({ ...draft, sourceUrl: event.target.value })} placeholder="粘贴大麦、纷玩岛或票星球演出链接" />\n              <button className="button ghost" type="button" disabled={recognizing || !draft.sourceUrl?.trim()} onClick={() => void recognizeSourceLink()}>{recognizing ? <Loader2 className="spin" /> : <Sparkles />}识别链接</button>\n              <label className="button ghost"><ImagePlus />截图识别<input type="file" accept="image/*" onChange={(event) => void recognizeScreenshot(event.target.files?.[0])} /></label>\n            </div>\n            {recognitionStatus && <small className="editor-recognition-status-v2">{recognitionStatus}</small>}\n          </EditorField>\n\n          <div className="media-upload-grid-v2 is-wide">\n            <MediaEditorCard label="主海报" kind="poster" items={mediaFor("poster")} state={mediaStates.poster} error={mediaErrors.poster} onFiles={addFiles} onRemove={removeMedia} />\n            <MediaEditorCard label="电子票根" kind="ticket" items={mediaFor("ticket")} state={mediaStates.ticket} error={mediaErrors.ticket} onFiles={addFiles} onRemove={removeMedia} />\n            <MediaEditorCard label="座位图" kind="seatMap" items={mediaFor("seatMap")} state={mediaStates.seatMap} error={mediaErrors.seatMap} onFiles={addFiles} onRemove={removeMedia} />\n            <MediaEditorCard label="现场精选" kind="livePhoto" items={mediaFor("livePhoto")} state={mediaStates.livePhoto} error={mediaErrors.livePhoto} multiple onFiles={addFiles} onRemove={removeMedia} />\n          </div>\n          <EditorField className="is-wide" label="曲目"><textarea value={draft.setlist.join("\\n")} onChange={(event) => setDraft({ ...draft, setlist: splitTextList(event.target.value) })} /></EditorField>\n          <EditorField className="is-wide" label="演出记录"><textarea value={draft.note || ""} onChange={(event) => setDraft({ ...draft, note: event.target.value })} /></EditorField>\n        </div>\n        <footer className="editor-footer-v2"><button className="button ghost" type="button" onClick={onCancel}>取消</button><button className="button primary" disabled={saving || mediaBusy || recognizing} type="submit">{saving ? <Loader2 className="spin" /> : <Check />}{mediaBusy ? "图片正在处理" : "保存记录"}</button></footer>\n      </form>\n    </div>\n  );\n}\n\nfunction EditorField'''
text = sub_once(text, r'export function RecordEditor\(.*?\nfunction EditorField', record_editor, "record editor media ui")

media_component = '''function mergeImportDraftIntoRecord(record: EventRecord, imported: ImportDraft): EventRecord {\n  const reliable = imported.importConfidence >= 0.4;\n  let media = record.media;\n  const mediaSource = imported.sourceChannel === "damai" ? "damai" as const : "external" as const;\n  if (imported.posterUrl) {\n    media = media.filter((item) => item.kind !== "poster").concat(makeMedia(record.id, "poster", imported.posterUrl, "公开海报", mediaSource));\n  }\n  if (imported.seatMapUrl) {\n    media = media.filter((item) => item.kind !== "seatMap").concat(makeMedia(record.id, "seatMap", imported.seatMapUrl, "座位图", mediaSource));\n  }\n  return {\n    ...record,\n    ...(reliable && imported.title ? { title: imported.title } : {}),\n    ...(reliable && imported.date ? { date: imported.date, status: imported.status } : {}),\n    ...(reliable && imported.time ? { time: imported.time } : {}),\n    ...(reliable && imported.city ? { city: imported.city } : {}),\n    ...(reliable && imported.venue ? { venue: imported.venue } : {}),\n    ...(reliable && imported.address ? { address: imported.address } : {}),\n    ...(reliable && imported.artists.length ? { artists: imported.artists, lineup: imported.artists.map((name) => ({ name, role: "artist" as const })) } : {}),\n    ...(reliable && imported.publicPriceRange ? { publicPriceRange: imported.publicPriceRange } : {}),\n    sourceChannel: imported.sourceChannel || record.sourceChannel,\n    sourceUrl: imported.sourceUrl || record.sourceUrl,\n    importConfidence: imported.importConfidence,\n    media,\n  };\n}\n\nfunction MediaEditorCard({\n  label, kind, items, state = "idle", error, multiple, onFiles, onRemove,\n}: {\n  label: string;\n  kind: MediaKind;\n  items: MediaAsset[];\n  state?: MediaProcessState;\n  error?: string;\n  multiple?: boolean;\n  onFiles: (kind: MediaKind, files: FileList | null) => Promise<void>;\n  onRemove: (id: string) => void;\n}) {\n  const status = state === "processing" ? "正在处理…" : state === "error" ? "处理失败" : items.length ? `已就绪 · ${items.length} 张` : "尚未添加";\n  return <article className={`media-editor-card-v2 is-${state}`}>\n    <header><strong>{label}</strong><span>{status}</span></header>\n    <div className="media-editor-thumbs-v2">\n      {items.length ? items.slice(0, 3).map((item) => <figure key={item.id}>\n        <OverlayMedia media={item} alt={item.title || label} />\n        <button type="button" aria-label={`移除${label}`} onClick={() => onRemove(item.id)}><X /></button>\n      </figure>) : <span className="media-editor-empty-v2"><ImagePlus />暂无{label}</span>}\n      {items.length > 3 && <span className="media-editor-more-v2">+{items.length - 3}</span>}\n    </div>\n    <footer>\n      <label><Upload />{items.length && kind !== "livePhoto" ? "更换" : "选择图片"}<input type="file" accept="image/*" multiple={multiple} onChange={(event) => void onFiles(kind, event.target.files)} /></label>\n      {error && <small>{error}</small>}\n    </footer>\n  </article>;\n}\n\n'''
text = replace_once(text, 'function UploadField({ label, kind, multiple, onFiles }: { label: string; kind: MediaKind; multiple?: boolean; onFiles: (kind: MediaKind, files: FileList | null) => Promise<void> }) {\n  return <label><Upload /><span>{label}</span><input type="file" accept="image/*" multiple={multiple} onChange={(event) => void onFiles(kind, event.target.files)} /></label>;\n}\n\n', media_component, "media editor card")

text = replace_once(text, '  const [loading, setLoading] = useState(false);', '  const [loading, setLoading] = useState(false);\n  const [ocrProgress, setOcrProgress] = useState("");', "import ocr progress")
ocr_import = '''\n  async function importScreenshots(files: FileList | null) {\n    if (!files?.length) return;\n    setLoading(true);\n    setOcrProgress("正在准备截图识别…");\n    try {\n      const nextDrafts: ImportDraft[] = [];\n      for (const file of Array.from(files)) {\n        const recognized = await recognizeTicketScreenshot(file, (progress, status) => setOcrProgress(`${status} ${Math.round(progress * 100)}%`));\n        nextDrafts.push(...await createDraftsFromText(recognized));\n      }\n      setDrafts(nextDrafts);\n      flash(`已从 ${files.length} 张截图识别 ${nextDrafts.length} 条草稿`);\n    } finally {\n      setLoading(false);\n      setOcrProgress("");\n    }\n  }\n'''
text = replace_once(text, '  return (\n    <div className="overlay-backdrop" onClick={onClose}>', ocr_import + '\n  return (\n    <div className="overlay-backdrop" onClick={onClose}>', "batch screenshot ocr")
text = replace_once(text, 'placeholder="粘贴大麦等演出链接、票务页文字或手机识别文字。多条内容可一次生成草稿。"', 'placeholder="粘贴大麦、纷玩岛、票星球演出链接，或票务页文字。也可直接使用下方截图识别。"', "import placeholder")
text = replace_once(
    text,
    '<div className="import-actions-v2"><button className="button primary" disabled={loading || !text.trim()} type="button" onClick={() => void parse()}>{loading ? <Loader2 className="spin" /> : <Sparkles />}识别草稿</button><label className="button ghost"><ImagePlus />批量图片<input type="file" accept="image/*" multiple onChange={(event) => void importImages(event.target.files)} /></label></div>',
    '<div className="import-actions-v2"><button className="button primary" disabled={loading || !text.trim()} type="button" onClick={() => void parse()}>{loading ? <Loader2 className="spin" /> : <Sparkles />}识别链接/文字</button><label className="button ghost"><ImagePlus />截图识别<input type="file" accept="image/*" multiple onChange={(event) => void importScreenshots(event.target.files)} /></label><label className="button ghost"><Upload />海报导入<input type="file" accept="image/*" multiple onChange={(event) => void importImages(event.target.files)} /></label>{ocrProgress && <span className="import-ocr-progress-v2">{ocrProgress}</span>}</div>',
    "import actions",
)
text = replace_once(text, '  return <img src={media.src} alt={alt || ""} onError={() => { setFailed(true); if (media.storagePath) window.dispatchEvent(new Event("live-memory:cloud-media-refresh")); }} />;', '  return <img src={media.src} alt={alt || ""} loading="lazy" decoding="async" onError={() => { setFailed(true); if (media.storagePath) window.dispatchEvent(new Event("live-memory:cloud-media-refresh")); }} />;', "lazy media decode")
text = replace_once(text, '  if (draft.posterUrl) media.push(makeMedia(record.id, "poster", draft.posterUrl, "公开海报", "damai"));\n  if (draft.seatMapUrl) media.push(makeMedia(record.id, "seatMap", draft.seatMapUrl, "座位图", "damai"));', '  const mediaSource = draft.sourceChannel === "damai" ? "damai" as const : "external" as const;\n  if (draft.posterUrl) media.push(makeMedia(record.id, "poster", draft.posterUrl, "公开海报", mediaSource));\n  if (draft.seatMapUrl) media.push(makeMedia(record.id, "seatMap", draft.seatMapUrl, "座位图", mediaSource));', "import media source")
write(path, text)

# overlays.css append styles
path = "src/overlays.css"
text = read(path)
if ".media-editor-card-v2" not in text:
    text += '''\n\n/* Mobile-first media editor feedback */\n.media-editor-card-v2 {\n  display: grid;\n  gap: 8px;\n  min-width: 0;\n  padding: 10px;\n  border: 1px solid var(--experience-border);\n  border-radius: 10px;\n  background: color-mix(in srgb, var(--experience-text) 3%, transparent);\n}\n.media-editor-card-v2 > header { display: flex; align-items: center; justify-content: space-between; gap: 8px; }\n.media-editor-card-v2 > header strong { font-size: 12px; }\n.media-editor-card-v2 > header span { color: var(--experience-muted); font-size: 10px; font-weight: 850; }\n.media-editor-card-v2.is-processing { border-color: #108879; }\n.media-editor-card-v2.is-error { border-color: #c04b45; }\n.media-editor-thumbs-v2 { display: flex; min-height: 76px; gap: 6px; align-items: center; overflow: hidden; }\n.media-editor-thumbs-v2 figure { position: relative; flex: 0 0 58px; width: 58px; height: 72px; margin: 0; overflow: hidden; border-radius: 7px; background: #101418; }\n.media-editor-thumbs-v2 figure > img, .media-editor-thumbs-v2 figure > span { width: 100%; height: 100%; object-fit: cover; }\n.media-editor-thumbs-v2 figure > button { position: absolute; top: 3px; right: 3px; display: grid; width: 20px; height: 20px; place-items: center; padding: 0; border: 0; border-radius: 50%; color: #fff; background: rgba(0,0,0,.68); }\n.media-editor-thumbs-v2 figure > button svg { width: 12px; height: 12px; }\n.media-editor-empty-v2 { display: grid; min-height: 72px; flex: 1; place-items: center; align-content: center; gap: 4px; color: var(--experience-muted); font-size: 10px; }\n.media-editor-empty-v2 svg { width: 22px; height: 22px; }\n.media-editor-more-v2 { font-size: 11px; font-weight: 900; color: var(--experience-muted); }\n.media-editor-card-v2 > footer { display: flex; align-items: center; justify-content: space-between; gap: 6px; }\n.media-editor-card-v2 > footer label { position: relative; display: inline-flex; align-items: center; gap: 5px; padding: 7px 9px; border-radius: 7px; color: var(--experience-text); background: color-mix(in srgb, var(--experience-accent-2) 9%, transparent); font-size: 10px; font-weight: 900; cursor: pointer; }\n.media-editor-card-v2 > footer label input { position: absolute; inset: 0; opacity: 0; cursor: pointer; }\n.media-editor-card-v2 > footer label svg { width: 15px; height: 15px; }\n.media-editor-card-v2 > footer small { color: #c04b45; font-size: 9px; }\n.editor-source-import-v2 { display: grid; grid-template-columns: minmax(0,1fr) auto auto; gap: 7px; align-items: center; }\n.editor-source-import-v2 > label { position: relative; }\n.editor-source-import-v2 > label input { position: absolute; inset: 0; opacity: 0; cursor: pointer; }\n.editor-recognition-status-v2 { color: #108879; font-size: 10px; line-height: 1.4; }\n.import-ocr-progress-v2 { align-self: center; color: var(--experience-muted); font-size: 10px; font-weight: 850; }\n@media (max-width: 720px) {\n  .media-upload-grid-v2 { grid-template-columns: repeat(2, minmax(0,1fr)); }\n  .editor-source-import-v2 { grid-template-columns: 1fr 1fr; }\n  .editor-source-import-v2 > input { grid-column: 1 / -1; min-height: 36px; }\n}\n@media (max-width: 480px) {\n  .media-upload-grid-v2 { grid-template-columns: 1fr; }\n  .media-editor-card-v2 { grid-template-columns: 92px minmax(0,1fr); align-items: center; }\n  .media-editor-card-v2 > header, .media-editor-card-v2 > footer { display: grid; align-content: center; }\n  .media-editor-thumbs-v2 { grid-row: 1 / span 2; min-height: 86px; }\n  .media-editor-thumbs-v2 figure { flex-basis: 68px; width: 68px; height: 86px; }\n}\n'''
write(path, text)

print("Applied mobile media editor, automatic sync, conflict, and ticket import repairs")
